% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_demographics_messy.R
\name{add_demographics_messy}
\alias{add_demographics_messy}
\title{Add Messy Demographics Columns}
\usage{
add_demographics_messy(
  dat,
  p_messy_age = 0.05,
  gender_probs = c(male = 0.3, female = 0.7),
  p_age_in_gender = 0.05,
  p_fmt_misspell = 0.1
)
}
\arguments{
\item{dat}{A data frame. Must contain at least one column (e.g., \code{id}).}

\item{p_messy_age}{Numeric scalar in \link{0,1}. Probability that \code{age} entries
are "messy" (spelled-out words like "twenty one" or replaced with \code{"male"}
or \code{"female"}). Defaults to 0.05.}

\item{gender_probs}{A named numeric vector giving probabilities for the base
gender categories (default: \code{c(male = 0.30, female = 0.70)}). Must sum to 1.}

\item{p_age_in_gender}{Numeric scalar in \link{0,1}. Probability that \code{gender}
entries are replaced by numeric ages instead of text genders. Defaults to 0.05.}

\item{p_fmt_misspell}{Numeric scalar in \link{0,1}. Probability that otherwise
correct genders are altered with case formatting or misspellings
(e.g., \code{"FEMALE"}, \code{"femail"}). Defaults to 0.10.}
}
\value{
A data frame containing all original columns of \code{dat}, plus two new
columns:
\describe{
\item{age}{Character vector of ages as numbers, spelled-out words, or
gender labels (depending on simulation).}
\item{gender}{Character vector of gender labels, sometimes replaced with
numbers, case variations, or misspellings.}
}
Columns are ordered with \code{id} first, followed by \code{age}, \code{gender}, then the
remaining columns of \code{dat}.
}
\description{
This function augments a data frame with simulated demographic variables
(\code{age}, \code{gender}) that intentionally include a mix of clean and messy entries.
It is useful for teaching data cleaning and preprocessing tasks.
}
\examples{
\dontrun{
set.seed(123)
df <- data.frame(id = 1:5)
add_demographics_messy(df)
}

}
