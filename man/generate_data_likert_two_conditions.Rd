% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_data_likert_two_conditions.R
\name{generate_data_likert_two_conditions}
\alias{generate_data_likert_two_conditions}
\title{Generate Likert-Scale Data for Two Experimental Conditions}
\usage{
generate_data_likert_two_conditions(
  n_per_condition = 100,
  factors = NULL,
  prefixes = NULL,
  alpha = 0.7,
  n_items,
  n_levels = 7,
  r_among_outcomes = 0.3,
  approx_d_between_groups = 0.5,
  condition_names = c("control", "treatment"),
  seed = NULL,
  lv_var = 1,
  x_var = 1,
  return_continuous = FALSE
)
}
\arguments{
\item{factors}{Character vector of latent factor names.}

\item{prefixes}{Character vector of item name prefixes.}

\item{alpha}{Numeric vector of target Cronbach's alpha reliabilities
for each factor.}

\item{n_items}{Integer vector giving the number of items per factor.}

\item{n_levels}{Integer; number of Likert response categories (default = 7).}

\item{lv_var}{Numeric; latent variance (default = 1).}

\item{x_var}{Numeric; item variance (default = 1).}

\item{corr_latent}{Either a single numeric correlation applied across all
latent pairs, or a correlation matrix.}

\item{d_target}{Numeric; Cohen’s d mean shift applied to all latents in
the treatment group (default = 0.5).}

\item{n_per_group}{Integer; number of participants per group (default = 1000).}

\item{ordered}{Logical; whether the Likert responses are returned as
ordered factors (default = TRUE).}
}
\value{
A data frame containing Likert-scale item responses for both
groups, with columns:
\itemize{
\item \code{id} — participant ID
\item \code{group} — experimental group ("control" or "treatment")
\item item responses (Likert-scale)
}
}
\description{
This function generates item-level Likert-scale data for two groups
(control and treatment), with a specified mean shift on the latent variables.
Data are simulated from a latent variable model using \code{lavaan::simulateData}
and then discretized into Likert categories with shared cutpoints.
}
\details{
The function builds a correlated latent factor model using
\code{.make_lavaan_kfactor_corr()}. Latent means are set to zero for the
control group and shifted by \code{d_target} for the treatment group.
Continuous data are then simulated via \code{lavaan::simulateData},
discretized into Likert scales with shared cutpoints across groups,
and returned as a single combined dataset.
}
\examples{
\dontrun{
dat <- generate_data_likert_two_conditions(
  n_items     = c(5, 6, 7),
  alpha       = c(.70, .75, .80),
  factors     = c("X1_latent","X2_latent","X3_latent"),
  prefixes    = c("X1_item","X2_item","X3_item"),
  corr_latent = 0.3,
  d_target    = 0.5,
  n_per_group = 500,
  n_levels    = 5
)
head(dat)
}

}
