% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_missingness_messy.R
\name{add_missingness_messy}
\alias{add_missingness_messy}
\title{Add Mixed "Messy" Missingness to X* Columns}
\usage{
add_missingness_messy(
  dat,
  proportion_missing = 0.05,
  pool = c(NA, "NA", -99, -999, "", "missing"),
  probs = NULL,
  col_pattern = "^X",
  seed = NULL
)
}
\arguments{
\item{dat}{A data frame.}

\item{pool}{Vector of replacement tokens. Defaults to
\code{c(NA, "NA", -99, -999, "")}.}

\item{probs}{Optional probabilities (same length as \code{pool}). If \code{NULL},
uses equal probabilities.}

\item{col_pattern}{Regex used to select columns (default: \code{"^X"}).}

\item{seed}{Optional integer seed for reproducibility.}

\item{prop}{Proportion of cells to replace in each targeted column (0–1).
Default \code{0.05}.}
}
\value{
A modified data frame where matching columns are coerced to
character and have approximately \code{prop} of their values replaced by
mixed missingness encodings.
}
\description{
Randomly replaces a proportion of entries in columns whose names begin with
\code{"X"} using a mixture of true missing values and messy encodings:
\code{NA}, \code{"NA"}, sentinel codes like \code{-99}/\code{-999}, and \code{""} (empty string).
Replacements are done independently \strong{per column}. Targeted columns are
coerced to character to accommodate mixed encodings.
}
\examples{
\dontrun{
set.seed(123)
df <- data.frame(
  id = 1:6,
  X1 = sample(1:5, 6, TRUE),
  X2 = sample(1:5, 6, TRUE),
  Y  = sample(1:5, 6, TRUE)
)
df_bad <- add_missingness_messy(df, prop = 0.15, seed = 42)
str(df_bad)   # note: X1/X2 are now character with mixed encodings
}

}
