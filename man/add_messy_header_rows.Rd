% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_messy_header_row.R
\name{add_messy_header_rows}
\alias{add_messy_header_rows}
\title{Add Undesirable Header Rows}
\usage{
add_messy_header_rows(
  data,
  headers = c("Study: depression RCT", "Data collected: 2025-08-17")
)
}
\arguments{
\item{data}{A data.frame or tibble.}

\item{headers}{Character vector of header rows to prepend (e.g., dates, notes).}
}
\value{
A character matrix that can be written to disk with \code{readr::write_lines()}
or a similar function. Each header row is one line of text, followed by the CSV data.
}
\description{
This function prepends arbitrary text rows above a data.frame's column names.
When written to disk, these rows will appear before the actual header row,
requiring the use of \code{readr::read_csv(skip = n)} to import cleanly.
}
\examples{
\dontrun{
dat <- data.frame(
  id = 1:3,
  score = c(10, 20, 15)
)
messy <- add_messy_header_rows(dat, c("Data collected: 2025-08-17", "Study: Pilot"))
readr::write_lines(messy, "messy.csv")
# To re-import: readr::read_csv("messy.csv", skip = 2)
}
}
