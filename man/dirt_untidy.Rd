% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dirt_untidy.R
\name{dirt_untidy}
\alias{dirt_untidy}
\title{Add an untidy column to a data frame}
\usage{
dirt_untidy(.data, col = "block_trial")
}
\arguments{
\item{.data}{A data frame.}

\item{col}{A string giving the name of the new column (default: \code{"block_trial"}).}
}
\value{
A data frame equal to \code{dat} but with an additional column named \code{col}.
}
\description{
This helper function adds a new column (by default called \code{"block_trial"})
that combines a randomly sampled block number and a sequential trial number.
The resulting column is a character vector in the format
\code{"block<block>_trial<trial>"} (e.g., \code{"block3_trial005"}), which breaks Tidy Data
principles.
}
\details{
\itemize{
\item Block numbers are sampled uniformly from 1 to 6, with one block number
assigned to the entire data frame.
\item Trial numbers are based on the row order of \code{dat} and zero-padded to 3 digits.
\item The function uses \code{dplyr::mutate()} and non-standard evaluation (NSE)
to create the column.
}
}
\examples{
\dontrun{
library(dplyr)

dat <- data.frame(id = 1:5)
dirt_untidy(dat)
dirt_untidy(dat, col = "trial_id")
}

}
