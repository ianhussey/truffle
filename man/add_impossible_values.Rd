% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_impossible_values.R
\name{add_impossible_values}
\alias{add_impossible_values}
\title{Add Impossible Values to Data}
\usage{
add_impossible_values(
  dat,
  proportion_impossible = 0.03,
  replacement_value = 99L
)
}
\arguments{
\item{dat}{A data frame. Assumed to contain integer columns, some of which
start with \code{"X"}.}

\item{prop}{A numeric value between 0 and 1 giving the proportion of values
to replace (default: 0.05).}

\item{value}{An integer to insert as the impossible value (default: 99).}
}
\value{
A modified data frame where a random proportion of values in
columns beginning with \code{"X"} have been replaced with \code{value}.
}
\description{
This function replaces a proportion of entries in integer columns
whose names begin with \code{"X"} with a specified impossible value.
This can be used to simulate data entry errors or invalid responses
in teaching datasets.
}
\examples{
\dontrun{
set.seed(123)
df <- data.frame(
  id = 1:5,
  X1 = sample(1:5, 5, replace = TRUE),
  X2 = sample(1:5, 5, replace = TRUE)
)
add_impossible_values(df, prop = 0.2, value = 99)
}

}
