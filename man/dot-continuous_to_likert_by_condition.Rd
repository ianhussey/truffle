% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/continuous_to_likert_by_condition.R
\name{.continuous_to_likert_by_condition}
\alias{.continuous_to_likert_by_condition}
\title{Discretize Continuous Data into Likert Scales by Condition}
\usage{
.continuous_to_likert_by_condition(
  .data,
  n_levels = 7,
  labels = NULL,
  ordered = TRUE,
  method = c("per_column", "fixed"),
  mu_ref = 0,
  sd_ref = 1
)
}
\arguments{
\item{.data}{A data frame or matrix of continuous variables (e.g., from
\code{lavaan::simulateData}).}

\item{n_levels}{Integer or vector specifying the number of Likert levels
per column (default = 7).}

\item{labels}{Either \code{NULL}, a character vector of category labels of
length equal to \code{n_levels}, or a list of such vectors (one per column).}

\item{ordered}{Logical; whether the output should be returned as ordered
factors (default = TRUE).}

\item{method}{Character; either \code{"per_column"} (z-standardize each
column separately) or \code{"fixed"} (apply fixed reference
\code{mu_ref}, \code{sd_ref}). Default is \code{"per_column"}.}

\item{mu_ref}{Numeric; the mean used for fixed standardization (default = 0).}

\item{sd_ref}{Numeric; the standard deviation used for fixed standardization
(default = 1).}
}
\value{
A data frame with discretized Likert-style variables.
}
\description{
This function converts continuous variables into Likert-style ordinal data
while preserving mean differences between experimental conditions.
Shared cutpoints are applied across groups to ensure comparability.
}
\details{
When \code{method = "per_column"}, each variable is standardized within
columns before discretization. This removes mean differences across groups.
When \code{method = "fixed"}, a shared reference mean and standard deviation
are used, preserving mean shifts across groups.
}
\examples{
\dontrun{
library(lavaan)
mod <- "F1 =~ x1 + x2 + x3"
dat <- simulateData(mod, sample.nobs = 100)
likert_dat <- .continuous_to_likert_by_condition(
  dat,
  n_levels = 5,
  ordered = TRUE,
  method = "fixed",
  mu_ref = 0,
  sd_ref = 1
)
head(likert_dat)
}

}
